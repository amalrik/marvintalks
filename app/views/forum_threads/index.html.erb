<h1 id="page_title">Main Thread Board</h1>

<%= render 'form' %>

<table>
  <tr>
    <th align="left">Assunto</th>
  </tr>
 
<% @forum_threads.each do |thread| %>
  <tr>
    <td><%= thread_link "#{thread.subject}", "#{thread.id}" %></td>
  </tr>
<% end %>
</table>